using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scene1bgm : MonoBehaviour
{
    public AudioSource bgm1;
    // Start is called before the first frame update
    void Start()
    {
        bgm1.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
